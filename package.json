{
  "name": "society-simulator",
  "version": "1.0.0",
  "description": "Hệ thống giả lập xã hội với AI",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "cli": "bun run src/cli.ts",
    "demo": "bun run src/demo.ts",
    "setup": "./setup-ollama.sh",
    "build": "bun build src/index.ts --outdir ./dist",
    "test": "bun test"
  },
  "dependencies": {
    "ollama": "^0.5.0",
    "@types/node": "^20.0.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.ts",
  "type": "module",
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}